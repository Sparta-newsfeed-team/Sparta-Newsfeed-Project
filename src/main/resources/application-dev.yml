spring:
  datasource:
    url: # jdbc:mysql://localhost:3306/{DATABASE}
    username: # {username}
    password: # {password}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true